[project]
name = ML service
ml_api_port = 8000
ml_api_host = localhost

; if run on docker, rename the host to the container name
[redis]
host = localhost
port = 6379
pass = password
db = 0


[rabbitmq]
host = localhost
post = 5672
user = guest
pass = guest
vhost = 


[celery]
query = ml_celery
queue = ml_celery
